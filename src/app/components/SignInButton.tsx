'use client'

import { Button } from '@mui/material'

export const SignInButton = () => {


    return (
        <Button
            variant='contained'
            color='primary'
            href='/sign-in'
        >
            Sign in
        </Button>
    )
}